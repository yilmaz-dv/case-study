{
  "name": "case-study",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"json-server --watch data.json --port=3000\" \"vite\"",
    "build": "vite build",
    "preview": "vite preview",
    "db:serve": "json-server --watch data.json --port=3000"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "axios": "^1.5.0",
    "bootstrap": "^5.3.2",
    "json-server": "^0.17.3",
    "pinia": "^2.1.6",
    "vue": "^3.3.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.2.3",
    "vite": "^4.4.5"
  }
}
